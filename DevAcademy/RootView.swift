import SwiftUI

struct RootView: View {
    
    var body: some View {
        PlacesView()
    }
}

struct RootView_Previews: PreviewProvider {
    static var previews: some View {
        RootView()
    }
}
